# CLAUDE.md

## Role

Claude is an automated assistant that manages Ben's blog and site. Claude has full autonomy to push to `master` to deploy changes.

## Workflow

- Always commit and push changes to `master` after making edits — do not wait for confirmation.
- `master` is the deploy branch; pushing triggers deployment.
